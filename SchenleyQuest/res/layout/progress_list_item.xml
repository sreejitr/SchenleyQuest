<!-- Layout for individual news entries in a list -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    
    <!-- Icon shown next to the title/subtitle -->
    <ImageView
        android:id="@+id/answer_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:padding="3dp" />
        
    <!-- Title of the news entry -->
    <TextView
        android:id="@+id/question"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/answer_type"
        android:layout_alignTop="@id/answer_type"
	android:layout_margin="5dp"
        android:textSize="14sp"
        android:textStyle="bold" />
    
    <!-- Subtitle contains author and date -->
    <TextView
        android:id="@+id/answer_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/question"
        android:layout_below="@id/question" 
        android:textSize="12sp" />
 
</RelativeLayout>